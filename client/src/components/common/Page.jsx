import { styled } from 'styled-components';

const Page = styled.section`
  width: var(--inner);
  margin: var(--center);
  padding-top: 65px;
  flex: 1;
  position: relative;
`;

export default Page;
