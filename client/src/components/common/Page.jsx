import { styled } from 'styled-components';

const Page = styled.section`
  width: var(--inner);
  margin: var(--center);
  padding-top: 65px;
  height: 100vh;
  position: relative;
`;

export default Page;
