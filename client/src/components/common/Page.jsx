import { styled } from 'styled-components';

const Page = styled.section`
  width: var(--inner);
  margin: var(--center);
  min-height: 100vh;
  position: relative;
  padding-top: 55px;
`;

export default Page;
