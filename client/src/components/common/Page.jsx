import { styled } from 'styled-components';

const Page = styled.section`
  width: var(--inner);
  margin: var(--center);
  min-height: calc(100vh - 75px);
  position: relative;
`;

export default Page;
