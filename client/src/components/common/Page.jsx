import { styled } from 'styled-components';

const Page = styled.section`
  width: var(--inner);
  margin: var(--center);
  padding-top: 65px;
  min-height: 100vh;
  position: relative;
`;

export default Page;
